<li>
  <span>
    <%= link_to a.trackable.title, question_path(a.trackable.id) %>
    <%= t "activity.update" %>
    <% if a.owner.present? %>
      <b class="owner_name"><%= a.owner.name %></b>
    <% end %>
  </span>
</li>
